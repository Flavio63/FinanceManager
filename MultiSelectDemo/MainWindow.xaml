<Window x:Class="MultiSelectDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiSelectDemo"
        xmlns:xx="clr-namespace:DAFc_library;assembly=DAFc_library"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <xx:MultiSelectComboBox Width="200" Height="50" ItemsSource="{Binding Items}"
                                  SelectedItems="{Binding SelectedItems}" Name="MC"/>

        <xx:ComboList Width="200" Height="50" Grid.Row="1"/>
    </Grid>
</Window>
